{
  id: @record.id,
  name: @record.name,
  description: @record.description,
  user_collection_id: @record.user_collection_id,
  x: @record.x,
  y: @record.y,
  width: @record.width,
  height: @record.height,
  angle: @record.angle,
  scale: @record.scale,
  border: @record.border,
  src: @record.src,
  color: @record.color,
  zindex: @record.zindex,
  type: "record"
}
