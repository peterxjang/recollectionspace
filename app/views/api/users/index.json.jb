@users.map do |user|
  {
    id: user.id,
    username: user.username,
    avatar: user.avatar,
    src: user.src,
    width: user.width,
    height: user.height,
    collections: user.user_collections
  }
end
